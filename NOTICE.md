# Distribution and third-party notice

**Project license: MIT** — see [LICENSE](LICENSE). Copyright (c) 2026 Untitled1-Agent.

The source package refers to Playwright, Chromium, FFmpeg, imageio-ffmpeg, Pillow and Python/Tk. They are obtained separately and carry their own licenses. The source ZIP does not bundle the browser, FFmpeg binary, Python environment, system fonts, customer HTML, rendered movies, or original customer artwork. Different FFmpeg builds can include different components; do not assume every packaged executable has the same distribution conditions.

The example HTML files and synthetic regression fixtures demonstrate the exporter without customer assets. Source renders used for private testing remain outside the repository. Reports identify test conditions without redistributing those inputs. Logs and projects can contain filenames, URLs, paths or page output: review/redact them before sharing.
