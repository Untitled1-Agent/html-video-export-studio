# Configuration reference

Generated by `python tools/generate_reference.py`. Do not hand-edit the tables.

Defaults below are **model defaults**. A recipe overrides them; see the recipe table.

Use the desktop Job Settings or the Python API for the complete model. The CLI exposes a subset.

## SourceSpec

| Field | Type | Default |
| --- | --- | --- |
| `value` | `str` | `"source.html"` |
| `kind` | `SourceKind` | `"file"` |
| `load_strategy` | `LoadStrategy` | `"auto"` |

## CaptureConfig

| Field | Type | Default |
| --- | --- | --- |
| `mode` | `CaptureMode` | `"auto"` |
| `selector` | `str` | `""` |
| `selector_index` | `int` | `0` |
| `viewport_width` | `int` | `1080` |
| `viewport_height` | `int` | `1920` |
| `manual_width` | `Optional[int]` | `null` |
| `manual_height` | `Optional[int]` | `null` |
| `geometry_mode` | `GeometryMode` | `"auto"` |
| `transparent_background` | `bool` | `false` |
| `strip_outer_shadow` | `bool` | `false` |

## TimelineConfig

| Field | Type | Default |
| --- | --- | --- |
| `mode` | `TimelineMode` | `"auto"` |
| `manual_duration` | `Optional[float]` | `null` |
| `trim_start` | `float` | `0.0` |
| `trim_end` | `Optional[float]` | `null` |
| `hold_start` | `float` | `0.0` |
| `hold_end` | `float` | `0.0` |
| `custom_event_name` | `str` | `"video-export-seek"` |
| `javascript_function` | `str` | `"window.seekTo"` |
| `realtime_start_delay` | `float` | `0.0` |

## RenderConfig

| Field | Type | Default |
| --- | --- | --- |
| `scale` | `float` | `1.0` |
| `fps` | `int` | `60` |
| `output_profile_key` | `str` | `"h264_420_mp4"` |
| `processing` | `ProcessingConfig` | `(nested settings)` |
| `audio` | `AudioConfig` | `(nested settings)` |
| `overwrite` | `bool` | `false` |
| `output_directory` | `str` | `""` |
| `save_next_to_source` | `bool` | `true` |
| `filename_template` | `str` | `"{stem}_{scale}x_{fps}fps_{profile}_{processing}.{ext}"` |

## ProcessingConfig

| Field | Type | Default |
| --- | --- | --- |
| `preset_key` | `str` | `"social_compensation"` |
| `sharpen_method` | `str` | `"cas"` |
| `sharpen_strength` | `float` | `0.28` |
| `contrast` | `float` | `1.0` |
| `saturation` | `float` | `1.0` |
| `brightness` | `float` | `0.0` |
| `deband` | `bool` | `false` |

## AudioConfig

| Field | Type | Default |
| --- | --- | --- |
| `path` | `str` | `""` |
| `mode` | `AudioMode` | `"none"` |
| `volume` | `float` | `1.0` |
| `offset_seconds` | `float` | `0.0` |
| `fade_in_seconds` | `float` | `0.0` |
| `fade_out_seconds` | `float` | `0.0` |

## Enumerations

**SourceKind:** `file`, `url`

**LoadStrategy:** `auto`, `loopback_http`, `file_url`, `embedded`, `remote_url`

**CaptureMode:** `auto`, `marker`, `selector`, `viewport`, `full_page`

**TimelineMode:** `auto`, `om_event`, `custom_event`, `javascript_function`, `web_animations`, `media`, `browser_clock`, `realtime`, `static`

**GeometryMode:** `auto`, `lock_intrinsic`, `preserve_layout`

**AudioMode:** `none`, `trim`, `loop`

**JobStatus:** `Queued`, `Analyzing`, `Ready`, `Rendering`, `Paused`, `Done`, `Error`, `Cancelled`, `Skipped`

## Recipes

| Key | Capture / timeline | Scale / fps | Codec | Processing | Explicit duration |
| --- | --- | --- | --- | --- | --- |
| `motion_graphics_master` | `auto` / `auto` | 2× / 60 | `lossless_rgb_mp4` | `auto_content_aware` | None; must resolve from source/user |
| `exact_source_master` | `auto` / `auto` | 2× / 60 | `lossless_rgb_mp4` | `no_processing` | None; must resolve from source/user |
| `editing_master` | `auto` / `auto` | 2× / 60 | `prores_4444_mov` | `no_processing` | None; must resolve from source/user |
| `social_delivery` | `auto` / `auto` | 1× / 60 | `h264_420_mp4` | `social_compensation` | None; must resolve from source/user |
| `web_animation` | `viewport` / `web_animations` | 1× / 60 | `h264_444_mp4` | `no_processing` | None; must resolve from source/user |
| `video_element` | `auto` / `media` | 1× / 60 | `h264_420_mp4` | `no_processing` | None; must resolve from source/user |
| `static_page_hold` | `viewport` / `static` | 1× / 30 | `h264_420_mp4` | `no_processing` | 5.0 |
| `website_capture` | `viewport` / `browser_clock` | 1× / 30 | `h264_420_mp4` | `no_processing` | None; must resolve from source/user |

## Processing presets

| Key | Method | Strength |
| --- | --- | --- |
| `auto_content_aware` | automatic selection | 0 |
| `no_processing` | none | 0 |
| `ui_subtle` | cas | 0.18 |
| `ui_balanced` | cas | 0.28 |
| `photo_gentle` | cas | 0.07 |
| `social_compensation` | cas | 0.28 |
| `custom` | none | 0 |

## Codec configuration

### `lossless_rgb_mp4`

Pixel-lossless RGB H.264 master (libx264rgb, CRF 0, 4:4:4). Best fidelity and compact compared with image sequences, but limited hardware-player compatibility. Not for TikTok uploads or reliable mobile playback; use Social delivery for those destinations.

Extension: `mp4`. Alpha: `False`. Even dimensions required: `False`.

Video arguments:
```text
-c:v libx264rgb -x264-params colorprim=bt709:transfer=iec61966-2-1:colormatrix=gbr:fullrange=on -crf 0 -preset medium -pix_fmt rgb24 -color_range pc -colorspace rgb -color_primaries bt709 -color_trc iec61966-2-1
```

Audio arguments:
```text
-c:a aac -b:a 320k
```

### `prores_4444_mov`

10-bit 4:4:4 ProRes editing master. Excellent interchange with NLEs; much larger than H.264. The current screenshot path is opaque RGB, so the alpha channel is filled unless transparent capture is used.

Extension: `mov`. Alpha: `True`. Even dimensions required: `False`.

Video arguments:
```text
-c:v prores_ks -profile:v 4 -pix_fmt yuva444p10le -vendor apl0 -color_primaries bt709 -color_trc iec61966-2-1 -colorspace bt709
```

Audio arguments:
```text
-c:a pcm_s24le
```

### `prores_hq_mov`

10-bit ProRes 422 HQ for editing workflows. Broad NLE support and high quality, with chroma subsampling by design.

Extension: `mov`. Alpha: `False`. Even dimensions required: `True`.

Video arguments:
```text
-c:v prores_ks -profile:v 3 -pix_fmt yuv422p10le -vendor apl0 -color_primaries bt709 -color_trc iec61966-2-1 -colorspace bt709
```

Audio arguments:
```text
-c:a pcm_s24le
```

### `h264_444_mp4`

Very high-quality H.264 with full chroma resolution. Smaller than the lossless RGB master, but not mathematically lossless and less compatible than ordinary 4:2:0 H.264.

Extension: `mp4`. Alpha: `False`. Even dimensions required: `False`.

Video arguments:
```text
-c:v libx264 -x264-params colorprim=bt709:transfer=iec61966-2-1:colormatrix=bt709:fullrange=off -crf 8 -preset slow -pix_fmt yuv444p -profile:v high444 -color_range tv -color_primaries bt709 -color_trc iec61966-2-1 -colorspace bt709
```

Audio arguments:
```text
-c:a aac -b:a 256k
```

### `h264_420_mp4`

8-bit H.264 Main for phones, VLC, browsers, and social uploads. 4:2:0 chroma subsampling may soften saturated text and fine UI edges.

Extension: `mp4`. Alpha: `False`. Even dimensions required: `True`.

Video arguments:
```text
-c:v libx264 -x264-params colorprim=bt709:transfer=iec61966-2-1:colormatrix=bt709:fullrange=off:open-gop=0 -crf 12 -preset slow -pix_fmt yuv420p -profile:v main -tag:v avc1 -refs 3 -bf 2 -g 120 -maxrate 20M -bufsize 40M -color_range tv -color_primaries bt709 -color_trc iec61966-2-1 -colorspace bt709
```

Audio arguments:
```text
-c:a aac -profile:a aac_low -b:a 256k -ar 48000 -ac 2
```

### `vp9_webm`

High-quality open WebM output with full chroma. Useful for web delivery; encoding is slower and editing support varies.

Extension: `webm`. Alpha: `False`. Even dimensions required: `False`.

Video arguments:
```text
-c:v libvpx-vp9 -crf 12 -b:v 0 -row-mt 1 -pix_fmt yuv444p -color_range tv -color_primaries bt709 -color_trc iec61966-2-1 -colorspace bt709
```

Audio arguments:
```text
-c:a libopus -b:a 192k
```

## CLI help

```text
usage: html-video-export [-h]
                         [--recipe {motion_graphics_master,exact_source_master,editing_master,social_delivery,web_animation,video_element,static_page_hold,website_capture}]
                         [--probe] [--deep-analysis] [--output OUTPUT] [--scale SCALE]
                         [--fps FPS]
                         [--profile {lossless_rgb_mp4,prores_4444_mov,prores_hq_mov,h264_444_mp4,h264_420_mp4,vp9_webm}]
                         [--processing {auto_content_aware,no_processing,ui_subtle,ui_balanced,photo_gentle,social_compensation,custom}]
                         [--capture {auto,marker,selector,viewport,full_page}]
                         [--selector SELECTOR]
                         [--timeline {auto,om_event,custom_event,javascript_function,web_animations,media,browser_clock,realtime,static}]
                         [--duration DURATION] [--trim-start TRIM_START]
                         [--trim-end TRIM_END] [--hold-start HOLD_START]
                         [--hold-end HOLD_END] [--viewport WIDTH HEIGHT]
                         [--geometry {auto,lock_intrinsic,preserve_layout}]
                         [--load {auto,loopback_http,file_url,embedded,remote_url}]
                         [--seek-function SEEK_FUNCTION] [--event-name EVENT_NAME]
                         [--overwrite]
                         sources [sources ...]

Analyze and export HTML animations/pages to video.

positional arguments:
  sources               HTML file path(s) or http(s) URL(s).

options:
  -h, --help            show this help message and exit
  --recipe {motion_graphics_master,exact_source_master,editing_master,social_delivery,web_animation,video_element,static_page_hold,website_capture}
                        Workflow recipe (default: social_delivery; sharp, compatible
                        MP4).
  --probe               Analyze only; write JSON to stdout.
  --deep-analysis       Sample frames for content classification.
  --output OUTPUT       Output file for one source, or directory for several sources.
  --scale SCALE
  --fps FPS
  --profile {lossless_rgb_mp4,prores_4444_mov,prores_hq_mov,h264_444_mp4,h264_420_mp4,vp9_webm}
  --processing {auto_content_aware,no_processing,ui_subtle,ui_balanced,photo_gentle,social_compensation,custom}
  --capture {auto,marker,selector,viewport,full_page}
  --selector SELECTOR
  --timeline {auto,om_event,custom_event,javascript_function,web_animations,media,browser_clock,realtime,static}
  --duration DURATION
  --trim-start TRIM_START
  --trim-end TRIM_END
  --hold-start HOLD_START
  --hold-end HOLD_END
  --viewport WIDTH HEIGHT
  --geometry {auto,lock_intrinsic,preserve_layout}
  --load {auto,loopback_http,file_url,embedded,remote_url}
  --seek-function SEEK_FUNCTION
                        JavaScript method path for function adapter.
  --event-name EVENT_NAME
                        Custom seek event name.
  --overwrite
```
