# Version 1.5.1

Install this repair in a fresh folder. It fixes startup failures and significant timing, pixel/color, queue and updater issues in the shipped 1.5.0 archive. Dynamic source duration, 2x/60fps defaults, optional clarity and an exact No processing RGB path remain.

The new TEST_REPORT.md separates actual Linux/browser/codec/GUI runs from unavailable native-platform, network-install and live GitHub checks. The raw validation logs are included. No customer animation, bundled font or generated master is included in the source archive.
