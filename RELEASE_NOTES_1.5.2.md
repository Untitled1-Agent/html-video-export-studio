# HTML Video Export Studio 1.5.2

Repository-preparation release, based on the supplied 1.5.1 source.

Fixed duration inference for mixed finite/infinite Web Animations; finite scene-duration overflow; external-audio/output collisions; preview input overwrite risks and partial preview publication; CLI probe JSON on startup failures; effective color/audio/PNG-preview capability selection; retained media-seek ElementHandles; Windows reserved output names; and standalone validation traversing local virtual environments or accepting missing CLI payloads. Update restart validation now happens before mutation.

Added an allowlisted release/publishing path so random root customer HTML is not packaged, a private-by-default GitHub publication helper with dry run and collision refusal, detailed user/API/configuration/architecture/release/security docs, generated-reference drift checks, issue/PR templates, Dependabot and expanded CI definitions.

Read [TEST_REPORT.md](TEST_REPORT.md) for executed evidence and limits. Native Windows/macOS live rendering/installers, live GitHub publication and release updates are not certified by local mocked tests. Known compatibility/follow-up items remain explicit.

Fresh-folder installation is recommended. Existing projects remain format version 1. No renderer duration fallback, AI upscaling, frame interpolation, or mandatory processing was added. Exact source master and No processing remain available.
